package com.fnklabs.dds;

import org.junit.Ignore;
import org.junit.Test;

public class BucketTest {

    @Test
    @Ignore
    public void testContains() throws Exception {
//        Bucket bucket = new Bucket(new byte[]{0, 0, 0}, new byte[]{0, 0, 0}, Bucket.State.OK);
//        boolean contains = bucket.contains(new byte[]{0, 0, 0});
//
//        Assert.assertTrue(contains);
//
//
//        bucket = new Bucket(new byte[]{0, 0, 0}, new byte[]{1, 0, 0}, Bucket.State.OK);
//        contains = bucket.contains(new byte[]{0, 0, 0});
//
//        Assert.assertTrue(contains);
//
//        bucket = new Bucket(new byte[]{0, 0, 0}, new byte[]{1, 0, 0}, Bucket.State.OK);
//        contains = bucket.contains(new byte[]{0, 0, 1});
//
//        Assert.assertTrue(contains);
//
//
//        bucket = new Bucket(new byte[]{0, 0, 0}, new byte[]{-75, 0, 0}, Bucket.State.OK);
//        contains = bucket.contains(new byte[]{0, 0, 1});
//
//        Assert.assertTrue(contains);
//
//        bucket = new Bucket(new byte[]{0, 0, 2}, new byte[]{-75, 0, 0}, Bucket.State.OK);
//        contains = bucket.contains(new byte[]{0, 0, 1});
//
//        Assert.assertFalse(contains);
    }
}